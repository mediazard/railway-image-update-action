engine-strict=true
save-exact=false
# Disable lifecycle scripts on install. Fork PRs can modify package-lock.json
# to point a transitive dev-dep at a malicious tarball; without this setting,
# `npm ci` would run that dep's postinstall on our CI runner.
ignore-scripts=true
